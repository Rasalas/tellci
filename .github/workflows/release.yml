name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: tellci-linux-x86_64
            binary: target/release/tellci
          - os: macos-latest
            artifact: tellci-macos-aarch64
            binary: target/release/tellci
          - os: windows-latest
            artifact: tellci-windows-x86_64.exe
            binary: target/release/tellci.exe

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Use stable Rust
        run: rustup default stable

      - name: Build release binary
        run: cargo build --release --locked

      - name: Stage artifact
        shell: bash
        run: |
          mkdir -p dist
          cp "${{ matrix.binary }}" "dist/${{ matrix.artifact }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: dist/${{ matrix.artifact }}
          if-no-files-found: error

  publish:
    name: Publish GitHub release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download artifacts
        uses: actions/download-artifact@v7
        with:
          path: dist

      - name: Stage artifacts
        run: |
          mkdir release
          cp install.sh release/
          find dist -type f -exec cp {} release/ \;
          ls -la release

      - name: Create checksums
        working-directory: release
        run: |
          for file in *; do
            sha256sum "$file"
          done | sort > SHA256SUMS
          cat SHA256SUMS
          for file in install.sh tellci-linux-x86_64 tellci-macos-aarch64 tellci-windows-x86_64.exe; do
            grep -qE "^[0-9a-f]{64}  ${file}$" SHA256SUMS || {
              echo "missing checksum entry for ${file}" >&2
              exit 1
            }
          done

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --repo "$GITHUB_REPOSITORY" \
            --title "$GITHUB_REF_NAME" \
            --generate-notes \
            $(find release -type f | sort)
